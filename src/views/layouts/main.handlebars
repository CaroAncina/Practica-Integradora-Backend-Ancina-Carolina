<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <header>
        {{#if user}}
        <p>Bienvenido, {{user.first_name}} {{user.last_name}} ({{user.role}}) | <a href="/api/sessions/logout">Cerrar
                sesión</a> | <a href="/profile">Perfil</a></p>
        {{/if}}
    </header>
    {{{ body}}}
</body>

</html>